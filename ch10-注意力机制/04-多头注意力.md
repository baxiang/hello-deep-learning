# 10.4 多头注意力

## 学习目标

- 理解多头注意力的设计思想
- 掌握多头注意力的计算过程
- 了解"多头"类比的实际意义

## 核心概念

### 多头注意力：多角度看问题

**单头注意力：只用一种方式看句子。**
**多头注意力：用多种方式看句子。**

类比：分析一篇文章时，你可能会从多个角度思考：
- **语法角度**：关注句子结构、主谓宾关系
- **语义角度**：关注词语含义、上下文联系
- **引用角度**：关注"他"、"这个"指的是什么
- **位置角度**：关注前后顺序关系

多头注意力就像有多个"专家"，每个专家关注不同的侧面。

### 多头的优势

| 特点 | 说明 |
|------|------|
| 多视角 | 不同的头关注不同的关系 |
| 并行计算 | 各头独立计算，可以并行 |
| 信息丰富 | 融合多种视角的信息 |
| 表达能力强 | 单头可能遗漏某些关系 |

### 多头注意力的结构

```
输入 X → 分成 h 个头
        ↓
    head_1: Q_1, K_1, V_1 → Attention_1 → output_1
    head_2: Q_2, K_2, V_2 → Attention_2 → output_2
    ...
    head_h: Q_h, K_h, V_h → Attention_h → output_h
        ↓
    拼接所有output → Concat → 线性变换 → 最终输出
```

每个头有自己的 QKV 权重，学习不同的注意力模式。

## 原理解析

### 多头注意力的计算

**1. 分头**

把输入 X 投影到多个子空间：

```
Q_i = X · W_q_i    # 第 i 个头的 Q
K_i = X · W_k_i    # 第 i 个头的 K
V_i = X · W_v_i    # 第 i 个头的 V
```

每个头的维度是 d_model / h。

**2. 各头独立计算注意力**

```
head_i = Attention(Q_i, K_i, V_i)
```

**3. 拼接并融合**

```
MultiHead = Concat(head_1, ..., head_h) · W_o
```

W_o 把所有头的信息融合起来。

### 为什么这样设计？

**不同头学习不同的模式：**

实验发现，多头注意力中：
- 有的头主要关注相邻词（局部依赖）
- 有的头主要关注句子两端（全局依赖）
- 有的头主要关注动词和主语
- 有的头主要关注形容词和名词

就像团队分工：每个成员负责一个方面，最后汇总。

### 头数的选择

Transformer 论文用 8 个头。

经验：
- 头太少（1-2）：表达能力不足
- 头太多（16+）：计算开销大，可能过拟合
- 通常 4-8 个头效果不错

## 代码实战

```python
import numpy as np


def softmax(x, axis=-1):
    """Softmax 函数"""
    e_x = np.exp(x - np.max(x, axis=axis, keepdims=True))
    return e_x / np.sum(e_x, axis=axis, keepdims=True)


class MultiHeadAttention:
    """多头注意力模块"""

    def __init__(self, d_model, num_heads):
        """初始化

        Args:
            d_model: 模型维度
            num_heads: 头的数量
        """
        assert d_model % num_heads == 0, "d_model 必须能被 num_heads 整除"

        self.d_model = d_model
        self.num_heads = num_heads
        self.d_k = d_model // num_heads  # 每个头的维度

        # 初始化各头的 QKV 权重
        np.random.seed(42)
        self.W_q = np.random.randn(d_model, d_model) * 0.1
        self.W_k = np.random.randn(d_model, d_model) * 0.1
        self.W_v = np.random.randn(d_model, d_model) * 0.1
        self.W_o = np.random.randn(d_model, d_model) * 0.1

    def forward(self, X):
        """前向传播

        Args:
            X: 输入序列，形状 (seq_len, d_model)

        Returns:
            output: 多头注意力输出
            all_weights: 所有头的注意力权重
        """
        seq_len = X.shape[0]

        # 计算 QKV
        Q = np.dot(X, self.W_q)  # (seq_len, d_model)
        K = np.dot(X, self.W_k)
        V = np.dot(X, self.W_v)

        # 分头：reshape 成 (num_heads, seq_len, d_k)
        Q_heads = Q.reshape(seq_len, self.num_heads, self.d_k).transpose(1, 0, 2)
        K_heads = K.reshape(seq_len, self.num_heads, self.d_k).transpose(1, 0, 2)
        V_heads = V.reshape(seq_len, self.num_heads, self.d_k).transpose(1, 0, 2)

        # 各头计算注意力
        all_weights = []
        all_outputs = []

        for h in range(self.num_heads):
            # 计算分数
            scores = np.dot(Q_heads[h], K_heads[h].T) / np.sqrt(self.d_k)

            # softmax
            weights = softmax(scores)
            all_weights.append(weights)

            # 加权求和
            head_output = np.dot(weights, V_heads[h])
            all_outputs.append(head_output)

        # 拼接所有头: (seq_len, d_model)
        concat = np.concatenate(all_outputs, axis=-1)

        # 最终线性变换
        output = np.dot(concat, self.W_o)

        return output, np.array(all_weights)


if __name__ == "__main__":
    print("=" * 50)
    print("多头注意力演示")
    print("=" * 50)

    np.random.seed(42)

    # 模拟句子
    seq_len = 4
    d_model = 16
    num_heads = 4

    words = ["我", "喜欢", "深度", "学习"]
    X = np.random.randn(seq_len, d_model)

    print(f"\n配置:")
    print(f"  模型维度: {d_model}")
    print(f"  头数量: {num_heads}")
    print(f"  每头维度: {d_model // num_heads}")

    # 多头注意力
    mha = MultiHeadAttention(d_model, num_heads)
    output, all_weights = mha.forward(X)

    print(f"\n输入形状: {X.shape}")
    print(f"输出形状: {output.shape}")
    print(f"权重矩阵数量: {len(all_weights)}")

    # 显示每个头的注意力
    print("\n=== 各头的注意力权重 ===")
    for h in range(num_heads):
        print(f"\n头 {h} 的注意力矩阵:")
        print("      ", end="")
        for w in words:
            print(f"{w:6s}", end=" ")
        print()
        for i, w in enumerate(words):
            print(f"{w:4s} ", end="")
            for j in range(seq_len):
                print(f"{all_weights[h][i, j]:.3f} ", end=" ")
            print()

    # 分析各头关注的不同模式
    print("\n=== 各头关注模式分析 ===")
    for h in range(num_heads):
        # 找每个词最关注的词
        max_attention = np.argmax(all_weights[h], axis=1)
        print(f"头 {h}: 各词最关注的目标")
        for i, w in enumerate(words):
            print(f"  '{w}' 最关注 '{words[max_attention[i]]}'")

    print("\n=== 多头注意力的意义 ===")
    print("1. 不同头关注不同的关系模式")
    print("2. 有的头关注局部（相邻词）")
    print("3. 有的头关注全局（远距离词）")
    print("4. 有的头关注语法（主谓关系）")
    print("5. 有的头关注语义（同义词）")
    print("6. 最后拼接融合所有视角的信息")

    print("\n" + "=" * 50)
    print("多头注意力总结:")
    print("多个头 = 多个专家")
    print("每个专家负责一个视角")
    print("最后汇总所有专家的意见")
    print("=" * 50)